<Window x:Class="ProjektManager.Views.NotizEingabeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Notiz eingeben" Height="220" Width="420" WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundBrush}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBox x:Name="NotizTextBox"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 Style="{StaticResource InputTextBoxStyle}"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Abbrechen" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,10,0" Click="Abbrechen_Click" />
            <Button Content="Speichern" Style="{StaticResource PrimaryButtonStyle}" Click="OK_Click" />
        </StackPanel>
    </Grid>
</Window>
